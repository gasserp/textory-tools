# Loaded by `vite build --mode native` (npm run build:native). Powers the
# Tauri desktop wrapper: token auth against the prod API instead of SWA cookies.
VITE_NATIVE=1
VITE_API_BASE=https://textory.dev
